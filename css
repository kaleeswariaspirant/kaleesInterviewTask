@charset "UTF-8";
body {
    font-family: Arial;
    padding: 40px;
}

input, select, textarea {
    width: 300px;
    margin: 5px 0;
    padding: 8px;
}

button {
    padding: 8px 15px;
    background: blue;
    color: white;
    border: none;
}

